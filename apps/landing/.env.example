# Opencom Widget Configuration
# Set these to enable the chat widget on the landing page

# Widget loader URL override.
# Default behavior: local dev uses /opencom-widget.iife.js, production uses https://cdn.opencom.dev/widget.js
NEXT_PUBLIC_WIDGET_URL=https://cdn.opencom.dev/widget.js

# Optional: Hosted/Opencom web app URL used by landing CTA (defaults to https://app.opencom.dev)
NEXT_PUBLIC_OPENCOM_WEB_APP_URL=

# Your Convex deployment URL
NEXT_PUBLIC_CONVEX_URL=

# Your Opencom workspace ID
NEXT_PUBLIC_WORKSPACE_ID=

# ─── Demo Seed ───────────────────────────────────────────────
# Populate the widget with demo content (tours, articles, checklists, etc.)
# Run from the repo root:
#   CONVEX_URL=<url> WORKSPACE_ID=<id> TEST_ADMIN_SECRET=<secret> pnpm seed:landing
# To remove seeded data:
#   CONVEX_URL=<url> WORKSPACE_ID=<id> TEST_ADMIN_SECRET=<secret> pnpm seed:landing:cleanup
# Requires ALLOW_TEST_DATA=true and TEST_ADMIN_SECRET set on the Convex deployment.
