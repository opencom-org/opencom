# Test environment configuration
# This file should be used when running tests against the test Convex deployment

# Convex test deployment URL
# Using dev deployment for now - ideally create a separate test project
CONVEX_URL=

# Site URL for Convex Auth (required for OTP email verification)
# Use http://localhost:3000 for local development
# Update to production URL when deploying
SITE_URL=http://localhost:3000

# Shared secret for calling internal test mutations via the testAdmin gateway action.
# Must match the TEST_ADMIN_SECRET env var set on the Convex deployment.
TEST_ADMIN_SECRET=

# Must be set to "true" on the Convex deployment to enable test data mutations
# npx convex env set ALLOW_TEST_DATA true

# To set up a dedicated test deployment:
# 1. cd packages/convex
# 2. npx convex login (if not already logged in)
# 3. npx convex dev --project opencom-test --once
# 4. Update CONVEX_URL above with the test deployment URL
# 5. Set SITE_URL in Convex dashboard: npx convex env set SITE_URL http://localhost:3000
# 6. Set ALLOW_TEST_DATA: npx convex env set ALLOW_TEST_DATA true
# 7. Set TEST_ADMIN_SECRET: npx convex env set TEST_ADMIN_SECRET <your-secret>
# 8. Copy the same secret into TEST_ADMIN_SECRET above

# Stripe billing (hosted mode)
# Add these to the Convex deployment and to packages/convex/.env.local when
# testing the private billing overlay locally.
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_RESTRICTED_ACCESS_KEY=
STRIPE_STARTER_PRICE_USD=
STRIPE_STARTER_PRICE_GBP=
STRIPE_PRO_PRICE_USD=
STRIPE_PRO_PRICE_GBP=
STRIPE_EMAIL_METERED_PRICE=
STRIPE_SEAT_METERED_PRICE=
